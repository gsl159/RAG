# ── Web Framework ─────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
gunicorn==22.0.0
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.3.1

# ── Database ──────────────────────────────────
sqlalchemy[asyncio]==2.0.30
asyncpg==0.29.0
aiosqlite==0.20.0
alembic==1.13.1

# ── Cache ─────────────────────────────────────
redis==5.0.4

# ── Vector DB ─────────────────────────────────
pymilvus==2.3.6
marshmallow==3.20.2       # pymilvus 依赖，固定版本防止冲突
environs==9.5.0           # pymilvus 依赖，固定版本防止冲突

# ── Object Storage ────────────────────────────
minio==7.2.7

# ── HTTP Client ───────────────────────────────
httpx==0.27.0

# ── Document Parsing（轻量）──────────────────
pymupdf==1.24.3            # PDF 解析，自带 MuPDF，无系统依赖
python-docx==1.1.2         # DOCX 解析，纯 Python
python-pptx==0.6.23        # PPTX 解析
openpyxl==3.1.5            # Excel 解析
# HTML / TXT / MD / CSV 用标准库处理
# OCR（可选，需 Tesseract 系统依赖）：
# pytesseract==0.3.10
# Pillow==10.3.0

# ── BM25 ──────────────────────────────────────
rank-bm25==0.2.2
elasticsearch==8.14.0      # BM25 外部化后端（可选）

# ── Auth & Security ───────────────────────────
PyJWT==2.8.0
bcrypt==4.1.3

# ── Utils ─────────────────────────────────────
loguru==0.7.2
python-dotenv==1.0.1

# ── Testing ───────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.7
pytest-mock==3.14.0
